rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Temporär: Erlaube allen Zugriff für Entwicklung
    // WICHTIG: Ändern Sie dies für Produktion!
    match /{allPaths=**} {
      allow read, write: if true;
    }
    
    // Für Produktion verwenden Sie diese Regeln:
    // match /customer-photos/{customerId}/{fileName} {
    //   allow read: if request.auth != null;
    //   allow write: if request.auth != null && request.resource.size < 10 * 1024 * 1024; // Max 10MB
    //   allow delete: if request.auth != null;
    // }
  }
}